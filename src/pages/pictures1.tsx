import type React from 'react';
import { PicturesBar } from '../components/PicturesBar';

const Pictures1: React.FC = () => {
    return (
        <div>
            <PicturesBar />
        </div>
    );
};

export default Pictures1;