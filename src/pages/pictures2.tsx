import type React from 'react';
import { PicturesMusic } from '../components/PicturesMusic';

const Pictures2: React.FC = () => {
    return (
        <div>
            <PicturesMusic />
        </div>
    );
};

export default Pictures2;